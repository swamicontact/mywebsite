name: SSH
on:
  push:
    branches:
      - main # or the name of your main branch

jobs:
  build:
    name: SSH
    runs-on: windows-latest
    steps:
      - name: SSH
        run: |
          ssh ${{ TEMPCOMMAND }}
