name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        password: ${{ secrets.SSH_PASSWORD }}
        port: ${{ secrets.SSH_PORT }}
        script: 
          copy C:\Users\tamilcloud1003\Downloads\mywebsite-2021-08-18-08-26-05.zip c:\inetpub\wwwroot\mywebsite
          Expand-Archive c:\inetpub\wwwroot\mywebsite\mywebsite-2021-08-18-08-26-05.zip c:\inetpub\wwwroot\mywebsite
